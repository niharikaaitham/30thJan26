package Encapsulation;

public class Theory {
    /*
    It’s one of the OOPs principles. Encapsulation in Java is a mechanism of wrapping(binding) the data members (variables) and member functions(methods) together into a single unit called a class.
In encapsulation, the variables of a class will be hidden from other classes by making access to variables private and can be accessed only through the public methods of the same class. Therefore, it is also known as data hiding.
To achieve encapsulation in Java −
·       Declare the variables of a class as private.
·       Provide public methods to access the data members.

     */
}
