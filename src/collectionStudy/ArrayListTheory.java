package collectionStudy;

public class ArrayListTheory {
    /*     List in Java provides the facility to maintain the ordered collection.
         1. Duplicate are allowed
         2. Allowed n number of null value
         3. Order of insertion is maintained

       The implementation classes of List interface are
        1.ArrayList   2.LinkedList    3.Vector    4.Stack

    * ArrayList is dynamic in nature means its size is growable in nature.
    * Default size of arraylist is 10.
    * ArrayList can contain duplicate elements.
    * ArrayList can contain more than one null value.
    * Arraylist maintains insertion order.
    * ArrayList class is non synchronized. - Not thread safe

    Syntax =>

    ArrayList a=new ArrayList();    => Dynamic array + Different Data Types

    ArrayList<Integer> a=new ArrayList<Integer>(); =>Static + Similar Data Type

    Methods =>

    	1).add();     => to add or insert the element in arrayList
    	2).remove();  => removes the element from arraylist
    	3).size();    => return the number of elements in arraylist
    	4).get(i);    => Retrieve the element  - charAt()
    	5).contains();=> Returns true if the list contains the specified element
    	6).set();     => Replace or change the value
    	7).IsEmpty(); => shows the value is empty or not
    	8).clone()
    	9).index()
*/

/*      ArrayList<Integer> b=new ArrayList<Integer>();

		b.add(10);   // only specific data types are used i,e. Integer/string/character
		b.add(30);   // combination of data types is not allowed i.e. int,char,string
		b.add(50);
		b.add(70);
		b.add(40);
		System.out.println(b);  */
}
